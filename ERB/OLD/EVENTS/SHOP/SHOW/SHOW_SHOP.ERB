@isMasterFree
#function
	returnf !FLAG:육변기형시작일

@SHOW_SHOP

	if FLAG:원정중
		PRINTL
		drawLine

		CALL continueExpeditionShopMenu
	endif
	PRINTL
	PRINTL
	DRAWLINE

	;계절에 대한 설명(넣어야함)
	PRINT 봄의 

	FONTBOLD
	SETCOLOR 0xffffff
	PRINTFORM {DAY}
	FONTREGULAR
	RESETCOLOR
	PRINT 일차 아침/밤 
	
	PRINT 소지금〈

	FONTBOLD
	SETCOLOR 0xcccc00
	printForm %TOSTR(MONEY,"#,###")%
	fontRegular
	resetColor
	
	print 〉 마석〈

	fontBold
	setColor 0x0077cc
	printForm %TOSTR(FLAG:마석,"#,###")%
	
	if !FLAG:마석
		print 없음
	endif
	fontRegular
	resetColor

	PRINTL 〉
	PRINTL
	
	if FLAG:육변기형시작일
		CALL showCumDump
	endif
	
	CALL showMenu
